// development env name
export const APP_DEVELOPMENT = 'development';
export const APP_NAME = 'api';

// production env name
export const APP_PRODUCTION = 'production';

// configuration namespace for root-level app config (eg. env mode, port, etc)
export const APP_CONFIG = 'app';
